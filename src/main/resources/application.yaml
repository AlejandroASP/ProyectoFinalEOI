spring:
  application:
    name : AlejandroSPEOI
  datasource:
    url: jdbc:mariadb://localhost/agames
    username: root
    password:
    driver-class-name: org.mariadb.jdbc.Driver
  web:
    resources:
      static-locations: file:public
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true